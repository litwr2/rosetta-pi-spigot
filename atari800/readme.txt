ENTER"H:PI.LST  or  LOAD"H:PI.BAS
RUN
press C when asked "RUN AGAIN?" to clear the screen, press Y to rerun, N to exit

You can use any valid drive letter instead of H.  However if another letter is used please edit line 30 of the Basic program - set a proper drive letter there.

The first run has to load the ML-program from a disk and it is really slow, it can take more than a minute.  The next runs start immediately.

PRINT CHR$(125) or press Home  -- screen clear
