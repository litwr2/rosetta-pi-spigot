F=$1
vasmm68k_mot -Fhunkexe -kick1hunks -databss -align -o ${F%.*} -L ${F%.*}.lst -nosym $F
#vasmm68k_mot -Fhunkexe -align -o ${F%.*} -L ${F%.*}.lst -nosym $F
#xdftool hello.ADF delete ${F%.*}
#xdftool hello.ADF write ${F%.*}
